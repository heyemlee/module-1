import { RouteSkeleton } from "@/features/platform/route-skeleton";

// Covers project detail and its nested round1 / renderings segments.
export default function Loading() {
  return <RouteSkeleton variant="detail" />;
}
