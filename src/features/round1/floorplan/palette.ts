// Shared SVG palette for the Round 1 floor-plan and elevation views. Centralized
// here so a color change lands in one place instead of being copy-pasted into
// each component (layout-preview, layout-preview-shapes, elevation-preview).
export const INK = "#1a1a1c"; // primary stroke — outlines, countertop & floor datum
export const LINE = "#3c3c3a"; // secondary hairlines
export const CABINET_FILL = "#ececea"; // cabinet body fill (plan + elevation)
